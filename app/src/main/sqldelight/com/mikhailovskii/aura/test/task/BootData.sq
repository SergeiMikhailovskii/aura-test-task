CREATE TABLE bootData (
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    bootTime INTEGER NOT NULL
);

selectAll:
SELECT * FROM bootData;

insertBootData:
INSERT INTO bootData(bootTime)
VALUES (?);
